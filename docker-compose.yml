version: '3.9'
services:
  forexmind-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend/artifacts:/app/backend/artifacts
    restart: unless-stopped
